# F5 (ticket 14) 진행 — 알림·외부 전달 tracer

> ⚠️ **SUPERSEDED — 이 문서의 "완료" 는 현재 코드에 대한 주장이 아니다 (2026-07-26 표기).**
> 여기서 완료로 기록한 notification-center · 인박스 표면 은(는) 2026-07-22 피벗의 Stage 1/2 컷에서 **삭제됐다.**
> 작성 시점에는 참이었고 기록으로서 유효하지만, **현재 능력의 근거로 인용하지 말 것.**
> 현행 범위는 [피벗 메모](../../../docs/notes/2026-07-22-pivot-backtest-strategy-engine.md)와
> [stage2-cleanup.md](./stage2-cleanup.md)가 정본이다.

Owner: main-agent. Claimed 2026-07-16T03:46:16+09:00. 기준 규칙: `docs/agents/collaboration.md`(프로젝트 내부 규칙), spec §10~12·F5·UF-08·SEC-05/06/09·AT-10/11.

## Blast-radius 프레이밍 (docs/agents/collaboration.md "검증 깊이와 blast radius")
- **되돌릴 수 없는 위험**: (1) 외부 egress(email/push가 실수신자에게), (2) exactly-once 위반 → 중복 외부 side effect(더블 email/push), (3) erasure 미완주 → 삭제 뒤 개인 delivery state 재생성.
- **Contain(핵심, blast radius 축소)**: 전 external adapter를 network-off scripted lane으로(실 Resend/VAPID/browser push는 opt-in flag-default-off, 미구성 시 인앱만 정상). Delivery Intent는 durable commit 후에만 발사 + `(causeId, channel, destinationFingerprint)` unique → idempotency. Delivery Fact append-only. Erasure fence + restore suppression(F4 `PersonalCacheStore`·F3 `ErasureParticipant` 패턴 재사용). → 최상위 자동승격이지만 contain 후 High-tier(blind test-authorship)로 충분.
- **Prevent(타입으로 불법상태 제거)**: 5-tuple Delivery Intent allowlist를 판별합집합으로 → 허용된 5개 (cause,channel,source?,action-material?,target) 외 조합은 표현 불가(typecheck 강제). 결과는 shared `InformationOutcome`/전용 outcome. endpoint/target/action-material/keyring/cause는 branded reference. exactly-once = serial rule watermark + unique 제약.
- **Detect(독립 oracle)**: `fixtures/spec/f5/**` 사람 검토 literal이 정본(고정 clock). **blind test-authorship**: exactly-once(100-concurrent, AT-10)과 erasure(SEC-09) acceptance는 구현 미열람·spec만으로 별도 에이전트가 작성. 내가 이번 변경에서 쓴 테스트는 decorrelation 근거로 안 침.

## 추론 강도 (collaboration.md "추론 강도")
- exactly-once/idempotency·동시성·삭제/복구·vault(credential material)·외부 egress 경로 = **XHigh**. 5-tuple allowlist·capability 합성·presentation = **High**. fixture·문구 = Medium.

## Tier (collaboration.md 표)
- **최상위 자동승격**: DeliveryActionMaterialVault/DeliveryKeyring(credential material), durable inbox/outbox(migration 경계), 외부 side-effect. → **contain(scripted+flag-off)으로 blast radius 낮춘 뒤 High 방법(blind test-authorship)로 충분**. contain 불가한 실 egress는 opt-in gate 뒤로 미루고 이번 PR엔 부재.
- money 산술 없음(전달 tracer). 실 provider/browser/email smoke는 opt-in contract flag+allowlist에서만(SEC-05/기밀 가드).

## 핵심 invariant (확정 시 사람 1회 검증)
1. rule별 직렬 false→true 전이 1개당 Alert Occurrence 1 + Notification Record 1(같은 transaction). stream/poll/replay·늦은 관측은 재생성 0·watermark 되돌림 0. (AT-10)
2. 100 concurrent same transition에서 occurrence/record 1, 허용 조합만 Intent, 외부 side effect ≤1.
3. Delivery Intent는 정확히 5개 (cause,channel,source?,action-material?,target) 조합만 생성. 그 외 variant는 Intent·renderer·provider 호출 0.
4. Delivery Fact는 append-only. provider accepted/open/click을 delivered/seen으로 승격 0. `sent` 추정 0.
5. Delivery Authorization Context(financial/challenge-pending/challenge-workspace/security-notice)는 render·dispatch 직전 purpose epoch·membership·address revision·deletion fence 재검사. stale/cross-purpose는 value 없는 rejected. (SEC-06)
6. PushTransport: exact host HTTPS:443·DNS 재검사·redirect 0·≤5 endpoint·≤4096B. (SSRF)
7. 비밀·원문 target·action material은 outbox/log/error/screenshot/AI prompt에 0(sentinel/secret pattern scan). (SEC-05)
8. administrative erasure 뒤 개인 delivery state(record/intent/fact/endpoint/material/webhook inbox/token hash/quota/abuse/cache/pending) 재생성 0, 늦은 queue/webhook/provider·backup restore도 재생성 0. module/processor receipt = coordinator 공개 상태. (SEC-09/AT-11)

## 검증 oracle
- `npm run check`(typecheck·lint·vitest·seam), worker/integration/browser test(범위 맞으면 check:f* / verify:network-off), spec §10~12 대조.
- fixed-clock fixture로 circuit/quota/half-open/Quiet Hours 결정성 확보.

## 배치 (각 = 체크포인트, npm run check green 후 다음)
- [x] **B1 Alert Occurrence exactly-once spine (AT-10 core)** — `notification-center/{contracts,occurrence-engine}.ts`. serial watermark(monotonic sourceObservationIdentity) + condition-revision 리셋 → false→true 1건당 Occurrence 1 + Notification Record 1. 단일 await 뒤 동기 atomic critical section이라 replay/late/out-of-order(identity ≤ watermark)는 stale로 무시, watermark rollback 0, 100-concurrent same transition도 exactly 1. Delivery Cause(`cause:alert:{rule}:{seq}`)가 dedupe/audit 정본. `tests/f5-occurrence.test.ts` 7 green(author oracle). **blind AT-10 검증 완료**: 별도 Sonnet 에이전트가 구현 미열람·spec만으로 `tests/f5-occurrence-acceptance.test.ts` 33 작성 → 33/33 green, 후보 버그 0, 3 mutation red→green(non-vacuous). durable DB unique 제약은 B7 실store로.
- [~] **B2 Delivery Intent 5-tuple allowlist (Prevent) + Auth Context 재검사**
  - [x] **Prevent allowlist**: `notification-center/delivery-intent.ts` — `planDeliveryIntent`가 정확히 5행(financial_email/financial_web_push/pending_account_challenge/workspace_account_challenge/authenticated_security_notice)만 planned, 그 외 조합은 생성 전 rejected(8개 사유). `uniqueKey=(causeId,channel,destinationFingerprint)`. `tests/f5-delivery-intent.test.ts` 19 green(5 허용 + 14 금지 매트릭스).
  - [ ] **미완(재배치됨)**: durable outbox `(causeId,channel,destinationFingerprint)` idempotent commit + Delivery Authorization Context 4종 *resolve*(발급). **SEC-06 dispatch-직전 재검사는 dispatch loop 필요 → B4로 이동.**

### 수정 시퀀스 (2026-07-16, 사용자 승인 — 우선순위 조정 4건)
조정 근거: (1) erasure fence는 구조적이어야 함(B6 bolt-on 금지) → substrate 앞으로, (2) B4 push/email 독립 slice 분리·병렬화, (3) vault=AT-11 명시 슬라이스·B4 선행, (4) SEC-06 재검사는 dispatch 경로로. B1 records가 지금 plain array라 fence-aware 아님 = 이미 쌓이는 부채.

- [x] **B2.5 fenced durable-store substrate (Prevent for SEC-09)** — `notification-center/fenced-store.ts`: `SubjectFence`(subject=workspace|pending identity monotonic fence) + `FencedKeyedStore<T>`(write/writeIfAbsent idempotent/list/erase, atEpoch ≤ fence write 억제) + `notificationErasureParticipant`(복합, 스토어별 receipt line = SEC-09 coordinator용 module receipt). `tests/f5-fenced-store.test.ts` 5 green(fence 억제·idempotent·subject 격리·participant). **미완**: B1 records/occurrences를 이 위로 이관(occurrence-engine 리팩터, epoch 배선) → B2.5b.
- [~] **B2 잔여**
  - [x] **durable outbox**: `delivery-outbox.ts` `DeliveryOutbox.commit`이 planned Intent를 `uniqueKey`로 idempotent commit(replay/100회 반복 → committed 1·duplicate 99), fence-aware(erased subject commit suppressed). `tests/f5-delivery-outbox.test.ts` 4 green(plan→commit 통합 포함).
  - [x] **B2.5b occurrence-engine 이관**: `occurrence-engine`의 records/occurrences를 `FencedKeyedStore`(subject=workspace)로 이관 + `writeEpoch` factory 배선(기본 1, 기존 인자 변경 0) + `eraseWorkspace(ws,fence)`(records·occurrences shred + rule state/watermark 삭제) + `registerRule` fence 가드(restore 재등록 차단). exactly-once 임계구역은 동기 fenced write로 보존. `tests/f5-occurrence-erasure.test.ts` 4 green, blind 33 + author 7 회귀 green(=44), mutation 2건(등록 fence 제거·occurrence 미shred) red 확인.
  - [x] **Delivery Authorization Context 4종 resolve**: `delivery-authorization.ts` — `resolveFinancialDelivery` / `resolveAccountChallengeDelivery`(pending·workspace 2 variant) / `resolveSecurityNoticeDelivery`가 durable record + Identity 현재 state에서 purpose-tagged context를 발급하고, spec §12(254/338/339)대로 deletion fence·membership/account state·stale epoch(financial=account-auth, notice=security-notice epoch 독립)·verified-address revision·financial channel consent(financial 전용)·pending identity(pending 전용)·expiry·**cross-purpose(pending↔workspace 교환, notice↔challenge)**를 재검사, 불일치는 전부 **value 없는 rejected**(target/endpoint 미유출). dispatch-직전 recheck는 B4가 동일 resolve 재호출. author oracle `tests/f5-delivery-authorization.test.ts` 21 green + **blind acceptance** `tests/f5-delivery-authorization-acceptance.test.ts` 54 green(별도 Sonnet 에이전트, 구현 미열람·spec+Interface Contract만, public API만 import 확인). mutation: 실제 보안 체크 전부 red(등가 mutation 1건은 fall-through로 동일 동작 확인), blind 파일 단독으로 3 mutation red. → **B2 완료.**
- [x] **B3 Delivery Fact append-only + Alert Channel Availability** — `delivery-fact.ts`: `DeliveryFactLog`이 11개 kind(queued/provider_accepted/delayed/delivered/seen/bounced/complained/provider_suppressed/suppressed/failed/expired)를 fresh sequence key로 fenced substrate에 append(update/promote API 부재 = 구조적 append-only), `projectDeliveryStatus`는 실제 기록된 kind만 precedence-max로 투영(accepted→delivered/seen 승격 0, `sent` 추정 0, terminal failure는 progress 위). erased subject append suppressed + shred(SEC-09). `channel-availability.ts`: 4축(supported/deploymentReady/consentAndAddressReady/permissionGranted/quotaAvailable)을 `unsupported>configuration_required>permission_denied>quota_blocked>ready` 우선순위로 합성. author oracle `tests/f5-delivery-fact.test.ts` 8 + `tests/f5-channel-availability.test.ts` 6 green. mutation: M1(accepted=delivered tie)이 갭 노출 → "accepted→delivered 진행" 테스트 보강 후 red 확인, append-overwrite·availability 2축 mutation red. Medium tier(순수 로직·append 기계, auth/egress 아님) → author+mutation 게이트.
- [x] **B3.5 vault (AT-11)** — `src/platform/delivery/`(F5 소유) 신설: `DeliveryVault`(seal/open/rewrap)가 F0의 `sealAes256Gcm/openAes256Gcm`(NIST 검증)·`CredentialKeyring`(=`DeliveryKeyring`, active/previous·notAfter)를 재사용하고 delivery 전용 `encodeDeliveryAad({kind,purpose,subject,reference})`만 신규 — kind(channel_endpoint↔action_material)·purpose·subject·reference를 AAD에 length-prefix 바인딩. `ChannelEndpointVault`·`DeliveryActionMaterialVault`는 kind 고정+reference brand 좁힌 타입 facade. **no plaintext fallback**(active key 부재→keyring throw, open 실패 전부 "delivery decryption unavailable"), active write·unexpired previous read·transactional rewrap(rollback시 원본 보존)·notAfter 후 fail closed. author `tests/f5-delivery-vault.test.ts` 14 green + **blind acceptance** `tests/f5-delivery-vault-acceptance.test.ts` 28 green(별도 Sonnet, impl 미열람·public index만). mutation: kind-단독 분리 갭 노출→테스트 보강 후 재검(M1 kind-drop·M5 facade kind confusion red), blind 단독으로 kind-drop·fail-open·nonce 3종 red. contain(in-memory 랜덤 KEK, 실 provider secret 0)+SEC-05 sentinel scan(envelope·error에 원문 0). **최상위 tier 게이트 충족**. (`.secrets` KEK/provider-secret 로딩 배선은 composition/B7.)
- [x] **B4a Push transport(SSRF/TTL/Topic)** — `push-transport.ts`: `resolvePushTarget`이 F0 SSRF primitive(`isPublicNetworkAddress`·`resolveProviderHostname`) 재사용 — exact-host HTTPS·default port(443)·registered hostname·DNS 재검사·사설망 차단(query 허용=WNS token, fragment 거부). redirect 0은 dispatch executor(`redirect:"error"`+주소 pinning) 재사용으로. `assertPushPayloadWithinLimit`(≤4096)·`assertPushEndpointBudget`(≤5). `classifyPushOutcome`: accepted/2xx→accepted, accept-before-timeout→accepted_unconfirmed, 404/410→subscription_inactive(retry 0), 413→payload·400→config permanent, 401/403→circuit_open(VAPID), 429→rate_limited, 5xx/network→retry, 그 외 4xx→fail closed. `pushNotificationTag(causeId)` 안정→retry 화면 알림 ≤1 수렴. `retry-schedule.ts`(financial+account 공용, §348): financial 즉시/30s/2m/10m/30m/90m max6·cap min(TTL,2h), account 즉시/5s/30s/2m/10m/30m/2h/6h max8·cap min(expiry,24h), 429 Retry-After는 지연만(앞당김 0)·deadline 초과시 stop. author `tests/f5-push-transport.test.ts` 9 + `tests/f5-retry-schedule.test.ts` 10 green, mutation 5종(SSRF·circuit·subscription·deadline·Retry-After) red. Medium tier(F0 SSRF 재사용+결정적 로직). **SEC-06 dispatch-직전 재검사 = B2 resolve를 B7 dispatch worker가 호출**(loop는 B7).
- [x] **B4b email circuit** — `email-quota.ts`: `reserveEmailQuota`가 total 100/일·3000/월, financial cap 75/일·2250/월·user 5/일·60/월로 security reserve(25/일·750/월)를 **구조적**으로 보장(financial≤75라 25는 항상 잔여), monthly는 daily보다 먼저 보고(suppress/expire). `email-circuit.ts`: `evaluateEmailCircuit`(sample≥200 & hard-bounce≥3% 또는 complaint≥0.05%→open, <200 & complaint≥1→warning·수동검토, else closed) + `emailCircuitProbe`(24h 후 half_open·manual→closed) + `isEmailAddressBlocked`(hard bounce→재검증 전 차단, complaint→suppress). `email-throttle.ts`: `financialCooldownActive`(same rule/instrument 30분), `quietHoursHoldsFinancial`(financial만 보류·security/in-app 미보류), `TokenBucket`(5rps). author `tests/f5-email-delivery.test.ts` 12 green, mutation 5종(reserve·minSample·bounce threshold·quiet-hours-only·half-open) red. Medium tier(결정적·고정clock·egress 0). 실 Resend 호출은 scripted adapter·opt-in gate 뒤(B7). → **B4 완료(push+email).**
- [x] **B5 Webhook inbox + RFC 8058 unsubscribe (AT-11)** — `webhook-ingress.ts`(POST+JSON·64 header/16KiB·256KiB body·2s deadline buffering 전 강제, peer 10rps/burst50 + global 50rps/burst100은 email TokenBucket 재사용, peer lane 먼저라 고갈 peer가 global 못 소진), `webhook-signature.ts`(svix scheme: id.ts.body HMAC-SHA256·whsec_ base64 key·v1 다중 candidate 회전·timingSafeEqual·±300s, **parse 전 raw 검증** — bad-sig+invalid-JSON→signature_mismatch로 순서 증명), `webhook-inbox.ts`(`(provider,environment,svix-id)` dedupe를 FencedKeyedStore 위에서, Fact는 **서버 저장** ProviderMessageDirectory의 owner/recipientFingerprint/templateRevision에 bind — webhook 주장 owner/address 미신뢰·미저장, open/click 승격 0, 미결합은 `unbound:provider|env` subject). **erasure tombstone registry**(spec 344): domain-separated versioned HMAC(raw provider id), active+미만료 previous key 전부로 raw 저장 전 조회, 일치→bounded counter만, 미만료 tombstone 참조 key retirement 거절, 복원 tombstone의 key 부재→raw storage fail closed(throw). `unsubscribe.ts`(spec 342/345): ≥256-bit token을 hash-only로 workspace/endpoint/topic/channel/consentRevision lineage에 bind(FencedStore·erasure shred·old-epoch 재발급 억제), GET 미소비·정확히 한 개 `List-Unsubscribe=One-Click` text field POST만 idempotent 소비, ingress URL 4KiB/body 8KiB/64 header/2s + IP-prefix 10/min·100/day + global 50/s 고정창, invalid audit은 token-미파생 `route|reason|minute|region` counter·20 samples/h·`r×s×g×1440+480` row cap+prune. **최상위 tier 게이트**(durable inbox 자동승격, scripted contain): author 24+14 green, mutation 5+5 kill, **blind acceptance 92**(별도 Sonnet, spec 342–345+Interface Contract만·구현/author 테스트 미열람·public import 검증) 단독으로 mutation 3종 kill. check 660/52 green. commit e6b344b.
- [x] **B6 Erasure coordinator + restore suppression (SEC-09)** — `notification-erasure.ts`: `NotificationCenterErasure`가 Identity coordinator(`requestAdministrativeErasure`, fence-first) 계약의 participant로 한 fence에 occurrence spine(rule/watermark/state/occurrence/record, `eraseWorkspace`) + 등록된 전 fenced store(intent outbox·fact log·webhook inbox·unsubscribe token hash·sealed endpoint/material)를 shred. directory binding을 owner 단위 제거+route 있는 provider message id를 같은 pass에서 tombstone → **late SIGNED webhook이 unbound lane이 아니라 erasure_tombstone으로 suppressed**(raw/inbox write 0·bounded counter만). route 없는 binding은 `unroutedBindings`로 정직 노출(은폐 0). `receiptFor` = 모듈 공개 erasure 상태(coordinator fence 동일·store별 실제 shred 수·replay는 원본 receipt 유지). `ProviderMessageDirectory.bind`에 optional route(하위호환)+`eraseOwner`. 게이트: author 5 green + mutation 5 kill(tombstone 미기록·미shred·fence 거짓·owner 필터 제거·replay 덮어쓰기), **blind acceptance 26**(별도 Sonnet, SEC-09+ticket 30/44행+Interface Contract만·구현/기존 f5 테스트 미열람·import 검증) 단독 mutation 3종 kill. check 691/54 green. **미포함(→B7)**: per-user quota/abuse·pending queue·alert cache store 등록(스토어 자체가 B7에서 생김), pending-identity subject의 account-scope 매핑(identity 측 mapping 필요), coordinator 실통합(AT-11 integration). scripted whsec_ 테스트 시크릿은 새 pre-commit format 스캔에 맞춰 런타임 조립로 전환(값 동일). commit 85fb108.
- [ ] **B7 presentation/browser + worker 배선 + perf** — 규모가 커서 3분할(각각 체크포인트 커밋):
  - [x] **B7a worker dispatch loop** — `dispatch-loop.ts` 구현 완료: durable outbox claim(동기 임계구역 — 100-concurrent tick에 외부 호출 정확히 1, 테스트 증명) → SEC-06 재검사(`authorize` 주입 계약 = B2 resolve, 매 attempt마다 fresh, rejected→suppressed fact·외부 호출 0) → email 게이트(address block→suppressed, circuit open→financial suppressed/security delayed+24h half-open probe, quota는 **메시지당 1회 예약**(TDD 중 발견: 시도당 예약이면 5xx retry가 user 5/day를 단일 메시지로 고갈 — mutation급 설계 결함 수정), monthly→suppressed·daily→다음 UTC 자정 hold, quiet hours financial-only hold, per-rule 30분 cooldown, token bucket은 fact 없는 transient defer) → push classify 전 매트릭스(2xx/accept-before-timeout→provider_accepted, 404/410→provider_suppressed+구독 비활성 콜백, 401/403/413/400→failed, 429/5xx→ladder retry) → **email accepted는 route 항상 포함 bind**(B6 unrouted 갭 폐쇄, eraseOwner가 route 반환 증명). retry queue(`FencedKeyedStore`)+quota ledger user counter = fenced(SEC-09 등록은 B7c). 게이트: author 28 green(red-first), mutation 5/5 kill(SEC-06 제거·claim 제거·route 탈락·accepted 재발송·Retry-After 무시) + 개발 중 라이브 red 1건(시도당 quota 예약). check 719/55 green. commit 9a28b0b.
  - [x] **B7b presentation/browser + perf** — `inbox-presenter.ts`(pure: 정본 record+append-only fact → view, promotion-free 라벨 — provider_accepted="발송 접수"·전달됨/확인됨은 기록된 fact에서만, WS-06 이름+tone 병기, dismissed 제외·unread 집계·aria-live announcement) + `OccurrenceStore.acknowledge`(read/dismiss가 record의 유일한 mutation, idempotent, fence 뒤 false·재생성 0) + `/f5-inbox` synthetic 표면(F4 선례: dev-only·force-dynamic·network-off, **실제** engine/outbox/dispatcher/presenter를 scripted adapter로 구동 — 100-concurrent 관측→DOM에서 occurrence/record=1 검증) + browser spec 10 green(desktop+mobile: 구조·AT-10 DOM·no-promotion·WS-06 이름 상태·실패해도 record 잔존) + perf: vitest 예산 3(observe→commit 500ms·commit→first claim 1.5s·inbox open 200/400ms, 500 records) + playwright paint 예산 2 lanes green(750/1200ms, spec 396). **B7a 결함 수정**: cooldown rule 키가 콜론 포함 ruleReference(`rule:r1`)에서 전 rule 공유로 뭉개지던 것 → seq 벗기기+회귀 테스트. mutation 3/3 kill(promotion 라벨·unread 무시·ack 오동작). check 732/57 green. commit 8532aca.
  - [x] **B7c AT-10/11 integration + blind 게이트** — `tests/f5-at10-11-integration.test.ts` 5 green: (1) AT-10 end-to-end — 100-concurrent 관측→allowlist→outbox(100 replay commit→committed 1)→100-concurrent dispatchTick→채널당 외부 호출 정확히 1·provider_accepted 정확히 1, 금지 조합은 outbox 전 rejected. (2) SEC-06 — dispatcher `authorize`를 **실제 B2 `resolveFinancialDelivery`**로 배선, durable commit 후 consent 철회/address revision 변경 → dispatch 시점 차단·provider 호출 0·suppressed. (3) AT-11 — **실제 IdentityService**(reauth→fence-first) coordinator에 NotificationCenterErasure participant 배선 + **B6 잔여 store 등록 완료**(email-quota-usage·dispatch-retry-queue), receipt fence=coordinator 공개 fence·라인별 shred>0·late tick 0 호출·late SIGNED webhook→erasure_tombstone·ack false·quota 재생성 0·resolver deletion_fenced, coordinator replay는 원본 receipt 유지. live mutation 2/2 kill(receipt 축소보고·participant 루프 제거). **blind 게이트**: 별도 Sonnet이 spec+공개 계약만으로 `tests/f5-b7-acceptance.test.ts` 45 작성(공개 경로 import만 — 검증됨), 42 green + 후보 버그 3건 보고 → **판정: 3건 모두 구현 버그 아님, 내 계약 문서 문구 모호성**(①② "usage stays 0"을 전역 집계까지로 해석 — SEC-09 대상은 per-user 키, 집계는 tombstone counter처럼 비개인 보존; ③ "leave the visible cards"의 leave 오독 — dismiss는 카드에서 제외·totalCount 보존). 판정대로 단언 3곳 수정+`require`→import 정규화(그 외 무변경), 45/45 green, **blind-단독 mutation 3/3 kill**(SEC-06 제거·promotion 라벨·tombstone 생략). check 782/59 green. commit 802877c.
  - **미포함(정직 기록)**: alert cache store는 F5 tracer에 실물이 없어(presenter가 pure) 등록 대상 자체가 부재. pending-identity subject의 account-scope 매핑은 identity 측 매핑 필요(cross-module) → ticket residual. worker 프로세스(src/worker) 상주 dispatch loop·durable DB store·`.secrets` DeliveryKeyring 실로딩은 opt-in 실배선으로 composition 통합 시점에(현 PR lane은 scripted·미구성 시 인앱만 정상 = spec Out-of-scope 준수).

## 진행 로그
- B1 완료(commit 예정). exactly-once spine green(7 test). 다음: blind AT-10 concurrency oracle 위임 → B2 Delivery Intent 5-tuple allowlist.
