# 사용자별 Provider Credential 암호화

서버 공용 Provider Credential은 환경변수에만 보관하고 사용자별 값은 서버에서 AES-256-GCM으로 봉투 암호화해 PostgreSQL에 저장한다. 브라우저에는 저장된 원문을 다시 반환하지 않으며 마지막 네 자리와 연결 상태만 노출한다. 구현은 키 관리 복잡도가 늘지만 데이터·AI·브로커 자격증명이 데이터베이스 유출이나 클라이언트 번들에 그대로 노출되는 위험을 줄인다.
