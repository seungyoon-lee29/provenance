# collaboration.md 커밋 프로토콜의 기계화 — 싼 검사부터 (fail fast)
# 콘텐츠 게이트(whitespace·credential·secret 파일)는 CI와 공유하는 한 소스다. ticket 22.
sh scripts/gates/content-gates.sh --cached || exit 1

# 게이트 생존성 — 선언만 있고 안 도는 게이트, 못 돌리는 레인 등록을 잡는다 (2026-07-26, arch-1)
sh scripts/gates/gate-liveness.sh || exit 1

# 전체 검증 게이트 — CI(`PR-fast`)와 동일하게 typecheck·lint·test.
# seams 2단계는 2026-07-26 제거됐다(런타임 no-op 이었다 — gate-ledger.txt).
# Next 번들 단계는 의도적으로 여기 없다: Dockerfile 의 이미지 빌드 스텝이 이미 돌리고
# CI `PR-integration` 이 그걸 모든 PR·push 에서 태운다. 근거는 gate-ledger.txt 를 볼 것.
# (전에는 "이 주석에 npm run build 를 쓰면 gate-liveness 가 없는 배선을 있다고 오판한다"
#  는 규율로 이름을 피해 적었다. 2026-07-27 부터 3b 가 주석 줄을 제외하고 `npm run <이름>`
#  호출 형태만 보므로 그 규율은 필요 없다 — 기계가 본다.)
npm run check

# 위 `npm run check` 는 워크트리를 본다. 부분 stage(= 이 저장소의 표준 절차)면 그 초록은
# 이 커밋의 트리에 대한 진술이 아니다. 인덱스가 워크트리와 다를 때만 따로 검사한다.
sh scripts/gates/staged-tree-check.sh || exit 1
