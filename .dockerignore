.env
.env.*
.secrets
.git
.next
node_modules
coverage
test-results
*.log
*.zip
.DS_Store

# 에이전트·툴 산출물 — 이미지에 실릴 이유가 없다. `.scratch/` 는 진행 기록·설계 메모·
# 적대 리뷰 판정이 들어 있는 내부 문서고인데 `COPY . .` 로 그대로 이미지에 실리고 있었다
# (2026-07-27, arch-2 B). `.git` 을 뺀 것과 같은 이유다.
# `.git` 제외의 대가는 tests/release/git-lane.ts 가 침묵 대신 선언으로 바꿔 놓았다 —
# compose pr-check 이 RELEASE_LANE_WITHOUT_GIT=1 로 축소된 커버리지를 명시한다.
.scratch
.claude
dist
playwright-report
